#include "Database.h"
using namespace std;

int main()
{
    Database database = Database();
    // Person person = Person();
    // Record record = Record();
    // Time time = Time();
    // Location location = Location();
}
